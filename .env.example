# ============================================
# LocalStack Configuration
# ============================================

# SQS Endpoint - Configure based on your LocalStack setup:
# - Using containerized LocalStack (make up): http://localstack:4566
# - Using external LocalStack (make up-no-localstack): http://host.docker.internal:4566
# - Local development: http://localhost:4566
SQS_ENDPOINT=http://localhost:4566

# AWS Region
AWS_REGION=us-east-1

# AWS Credentials (for LocalStack, these can be dummy values)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# ============================================
# Backend Configuration
# ============================================
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# ============================================
# Frontend Configuration
# ============================================

# Backend API URL (used by Vite during build)
VITE_BACKEND_URL=http://localhost:8000

# ============================================
# LocalStack Data Directory
# ============================================

# Directory to persist LocalStack data
LOCALSTACK_VOLUME_DIR=./localstack-data

# ============================================
# External LocalStack Container Configuration
# ============================================
# Use these settings when connecting to an existing LocalStack container
# in a different Docker network

# Docker network name where your LocalStack container is running
# Example: myproject_default, localstack_network, etc.
#LOCALSTACK_NETWORK_NAME=localstack-network

# Name of your existing LocalStack container
# This should match the container name or service name in your LocalStack setup
#LOCALSTACK_CONTAINER_NAME=localstack

# When using external LocalStack, set SQS_ENDPOINT to match your container name:
# SQS_ENDPOINT=http://your-localstack-container-name:4566
